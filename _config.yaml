# title: "HOANG Minh-Ha"
# description: "Data Engineer | Data Analyst"
markdown: GFM
remote_theme: "mmistakes/minimal-mistakes@4.27.3"
plugins:
  - jekyll-remote-theme
  - jekyll-include-cache

# Affiche la sidebar avec profil
author:
  name: "HOANG Minh-Ha"
  avatar: "/assets/images/avatar.png"
  bio: "Data Engineer | Data Analyst"
  location: "Versailles, FR"
  links:
    - label: "E-mail"
      icon: "fas fa-envelope"        # Font Awesome
      url: "mailto:mhoang86@gmail.com"
    - label: "LinkedIn"
      icon: "fab fa-linkedin"
      url: "https://www.linkedin.com/in/minh-ha-hoang/"
    - label: "GitHub"
      icon: "fab fa-github"
      url: "https://github.com/vimchun/"

# Mettre le profil auteur par défaut sur toutes les pages
# defaults:
#   - scope:
#       path: ""
#       type: pages
#     values:
#       author_profile: true

search: false  # barre de recherche

minimal_mistakes_skin: "neon" # "air", "aqua", "contrast", "dark", "default", "dirt", "neon", "mint", "plum", "sunrise"


# theme: jekyll-theme-leap-day  # title/description (OK), ressemble à la doc python, sections cliquables sur volet gauche (mais trop de sections)
# theme: jekyll-theme-hacker # title/description (OK), lien url (OK), niveau des sections (KO), icône section dépliée (OK), black theme (OK)
# theme: jekyll-theme-modernist # title/description (OK), lien url (OK), niveau des sections (KO), icône section dépliée (OK)
# theme: jekyll-theme-slate # title/description (OK), lien url (OK), niveau des sections (KO), icône section dépliée (OK)
# theme: jekyll-theme-architect # title/description (OK), lien url (OK), niveau des sections (OK), icône section dépliée (KO)
# theme: jekyll-theme-merlot  # title/description (OK), icône section dépliée (OK), theme qui fait un peu "diplome", commentaires en gras
# theme: jekyll-theme-midnight  # black theme (OK), icône section dépliée (OK), niveau des sections (KO)
# theme: jekyll-theme-cayman  # title/description (OK), banière verte qui prend trop de place
# theme: jekyll-theme-minimal  # prend une grosse partie à gauche pour rien, icône section dépliée (OK)


# https://github.com/pages-themes/leap-day